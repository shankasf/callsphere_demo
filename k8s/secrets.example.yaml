# TEMPLATE ONLY — do NOT commit real values.
# The real Secret is created out-of-band from the .env files, e.g.:
#
#   kubectl create secret generic helloreclaim-app-secrets -n helloreclaim \
#     --from-literal=DATABASE_URL='postgresql://postgres:PASSWORD@72.62.162.83:5432/helloreclaim' \
#     --from-literal=OPENAI_API_KEY='sk-...' \
#     --from-literal=INTERNAL_API_KEY='hr-internal-...' \
#     --from-literal=TWILIO_ACCOUNT_SID='AC...' \
#     --from-literal=TWILIO_AUTH_TOKEN='...' \
#     --from-literal=TWILIO_PHONE_NUMBER='+1...' \
#     --from-literal=TWILIO_TWIML_APP_SID='AP...' \
#     --from-literal=TWILIO_API_KEY_SID='SK...' \
#     --from-literal=TWILIO_API_KEY_SECRET='...'
apiVersion: v1
kind: Secret
metadata:
  name: helloreclaim-app-secrets
  namespace: helloreclaim
type: Opaque
stringData:
  DATABASE_URL: "postgresql://postgres:PASSWORD@72.62.162.83:5432/helloreclaim"
  OPENAI_API_KEY: "sk-REPLACE"
  INTERNAL_API_KEY: "hr-internal-REPLACE"
  TWILIO_ACCOUNT_SID: "AC-REPLACE"
  TWILIO_AUTH_TOKEN: "REPLACE"
  TWILIO_PHONE_NUMBER: "+1REPLACE"
  TWILIO_TWIML_APP_SID: "AP-REPLACE"
  TWILIO_API_KEY_SID: "SK-REPLACE"
  TWILIO_API_KEY_SECRET: "REPLACE"
